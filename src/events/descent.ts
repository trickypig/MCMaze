import type { RunState } from "../state/run";

export function beginDescent(_state: RunState): void {
  // Stub — real implementation lands in Task 11.
}

export function isDescentInFlight(): boolean {
  return false;
}
